<DOCTYPE html>
  <head>
    <meta charset='UTF-8'>
    <title>Most Popular Hour of Day to Register</title>
  </head>
  <body>
    <h1>Most Popular Day to Register</h1>
   
    <table>
      <% if times.kind_of?(Hash) %>
      <th>Hour</th><th>Frequency</th>
        <% times.each_key do |hour| %>
        <tr>
          <td><%= "#{hour}" %></td>
          <td><%= "#{times[hour]}" unless times[hour].nil? %> </td>
        </tr>
        <% end %>
        <% else %>
        <% end %>
  
    </table>
  </body>
  </html>